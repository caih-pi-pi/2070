STACK SEGMENT STACK
	DW 64 DUP(?)
STACK ENDS
CODE SEGMENT
	ASSUME CS:CODE
START:	MOV SI,3000H
	MOV CX,0008H
	CALL branch
	INT 03H
branch:	JCXZ A4
	PUSH SI
	PUSH CX
	PUSH BX
	MOV BH,[SI]
	MOV BL,BH
A1:	LODSB
	CMP AL,BH
	JBE A2
	MOV BH,AL
	JMP A3
A2:	CMP AL,BL
	JAE A3
	MOV BL,AL
A3:	LOOP A1
	MOV AX,BX
	POP BX
	POP CX
	POP SI
A4:	RET
CODE ENDS
     END START